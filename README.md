# 🚀 Система Управления Банковскими Картами

Backend-приложение на Java (Spring Boot) для управления банковскими картами с поддержкой аутентификации, авторизации, шифрования данных и переводов между картами.

## 📋 Содержание

- [Технологии](#технологии)
- [Функциональность](#функциональность)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [API Документация](#api-документация)
- [Тестирование](#тестирование)

## 💡 Технологии

- **Java 17+**
- **Spring Boot 3.2.0**
- **Spring Security** - аутентификация и авторизация
- **JWT** - токены для аутентификации
- **Spring Data JPA** - работа с базой данных
- **PostgreSQL** - база данных
- **Liquibase** - миграции базы данных
- **Docker & Docker Compose** - контейнеризация
- **Swagger/OpenAPI** - документация API
- **Maven** - управление зависимостями

## ✨ Функциональность

### Аутентификация и авторизация
- JWT-based аутентификация
- Роли: `ADMIN` и `USER`
- Безопасное хранение паролей (BCrypt)

### Возможности администратора
- Создание, блокировка, активация, удаление карт
- Управление пользователями
- Просмотр всех карт в системе

### Возможности пользователя
- Просмотр своих карт (поиск + пагинация)
- Запрос блокировки карты
- Переводы между своими картами
- Просмотр баланса

### Безопасность
- Шифрование номеров карт (AES)
- Маскирование номеров карт в ответах (формат: `**** **** **** 1234`)
- Ролевой доступ к ресурсам
- Валидация входных данных

## 📦 Требования

- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose
- PostgreSQL 15+ (или использовать Docker Compose)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd bank_rest
```

### 2. Запуск PostgreSQL через Docker Compose

```bash
docker-compose up -d
```

Это запустит PostgreSQL на порту 5432 с следующими параметрами:
- База данных: `bankcards`
- Пользователь: `postgres`
- Пароль: `postgres`

### 3. Настройка приложения

Конфигурация находится в `src/main/resources/application.yml`. По умолчанию используются следующие настройки:

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bankcards
    username: postgres
    password: postgres

server:
  port: 8080
```

### 4. Запуск приложения

#### Вариант 1: Через Maven

```bash
mvn clean install
mvn spring-boot:run
```

#### Вариант 2: Через JAR

```bash
mvn clean package
java -jar target/bank-cards-1.0.0.jar
```

Приложение будет доступно по адресу: `http://localhost:8080`

### 5. Инициализация данных

При первом запуске автоматически создаются:
- Роли: `ROLE_ADMIN`, `ROLE_USER`
- Администратор: `admin` / `admin123`
- Тестовый пользователь: `user` / `user123`

## 📚 API Документация

После запуска приложения документация Swagger доступна по адресу:

- Swagger UI: http://localhost:8080/swagger-ui.html
- OpenAPI JSON: http://localhost:8080/api-docs

### Основные эндпоинты

#### Аутентификация
- `POST /api/auth/login` - Вход в систему

#### Карты (требуется аутентификация)
- `GET /api/cards` - Получить список своих карт (с фильтрацией и пагинацией)
- `GET /api/cards/{id}` - Получить карту по ID
- `POST /api/cards` - Создать новую карту
- `PATCH /api/cards/{id}/status` - Изменить статус карты
- `DELETE /api/cards/{id}` - Удалить карту (только для админа)
- `POST /api/cards/transfer` - Перевод между своими картами
- `GET /api/cards/all` - Получить все карты (только для админа)

#### Администратор
- `GET /api/admin/users` - Получить список всех пользователей
- `DELETE /api/admin/users/{id}` - Удалить пользователя

### Пример использования

#### 1. Вход в систему

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

Ответ:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "type": "Bearer",
  "id": 1,
  "username": "admin",
  "email": "admin@example.com",
  "roles": ["ROLE_ADMIN", "ROLE_USER"]
}
```

#### 2. Создание карты (используйте токен из предыдущего шага)

```bash
curl -X POST http://localhost:8080/api/cards \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "cardNumber": "1234567890123456",
    "cardholderName": "John Doe",
    "expiryDate": "2026-12-31"
  }'
```

#### 3. Перевод между картами

```bash
curl -X POST http://localhost:8080/api/cards/transfer \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "fromCardId": 1,
    "toCardId": 2,
    "amount": 100.00
  }'
```

## 🧪 Тестирование

Запуск юнит-тестов:

```bash
mvn test
```

Основные тесты покрывают:
- Переводы между картами
- Маскирование номеров карт
- Валидацию бизнес-логики

## 📁 Структура проекта

```
bank_rest/
├── src/
│   ├── main/
│   │   ├── java/com/example/bankcards/
│   │   │   ├── config/          # Конфигурация (Security, OpenAPI, DataInitializer)
│   │   │   ├── controller/      # REST контроллеры
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── entity/          # JPA сущности
│   │   │   ├── exception/       # Исключения и обработчики
│   │   │   ├── repository/      # Spring Data репозитории
│   │   │   ├── security/        # JWT и Spring Security
│   │   │   ├── service/         # Бизнес-логика
│   │   │   └── util/            # Утилиты (шифрование, маскирование)
│   │   └── resources/
│   │       ├── application.yml  # Конфигурация приложения
│   │       └── db/migration/    # Liquibase миграции
│   └── test/                    # Тесты
├── docker-compose.yml           # Конфигурация Docker Compose
├── pom.xml                      # Maven зависимости
└── README.md                    # Документация
```

## 🔒 Безопасность

- Номера карт шифруются в базе данных с использованием AES
- В ответах API номера карт маскируются (показываются только последние 4 цифры)
- Пароли хешируются с использованием BCrypt
- JWT токены для stateless аутентификации
- Ролевой доступ к ресурсам

## 📝 Примечания

- Для продакшена обязательно измените `jwt.secret` в `application.yml` на случайную строку
- Настройки подключения к БД можно переопределить через переменные окружения
- Для большей безопасности рекомендуется использовать SSL/TLS в продакшене

## 🤝 Лицензия

Apache License 2.0
